# Weston configuration for RuneLite MCP
# Copy this to ~/.config/weston.ini to customize weston behavior

[shell]
# Hide the panel for a cleaner fullscreen experience
panel-position=none

# Optional: Set a background image (leave commented to use default)
# background-image=/path/to/image.png

[general]
# Use fullscreen shell mode (no borders, maximized)
# Uncomment the line below if you want pure fullscreen
# shell=fullscreen
