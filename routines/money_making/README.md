# routines/money_making/

Placeholder directory — no routines exist here yet.

This directory is reserved for money-making routines (flax picking, cowhide
collection, wine grabbing, rune crafting, etc.). See the "Planned" →
"Money Making" table in `../../ROUTINE_CATALOG.md` for the current backlog
and `../../ROUTINE_SCHEMA.md` for the YAML schema to author against.

Before writing a routine here: check `ROUTINE_CATALOG.md` to confirm it's
still unimplemented (the catalog is the source of truth, not this file),
then follow the "Authoring a new routine" steps at the bottom of that
document.
