mcp>=1.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
watchdog>=3.0.0

# Discord bot
discord.py>=2.3.0

# LLM providers (install what you need)
google-generativeai>=0.3.0  # Gemini (cheapest)
anthropic>=0.50.0           # Claude (manny-driver default)
# openai>=1.0.0             # OpenAI (uncomment if needed)
httpx>=0.24.0               # Ollama HTTP client
